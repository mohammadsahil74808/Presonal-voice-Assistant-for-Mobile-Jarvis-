# Execution Scripts (Layer 3: Deterministic Code)

This directory contains deterministic Python/shell scripts and automation tools.

## Principles
- Handle API calls, data processing, file operations, and build tasks reliably.
- Use environment variables from `.env` for secrets/tokens.
